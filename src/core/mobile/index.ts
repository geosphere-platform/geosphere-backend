/**
 * GeoSphere Core Mobile Backend Infrastructure — Domain Entrypoint
 *
 * Framework-independent Mobile Backend Infrastructure providing device registration,
 * session revocation, remote app configuration, delta sync processing, dynamic field forms,
 * field task workflow state machines, chunked media uploads, and offline map region packages.
 */

export * from "./types/mobile-backend.types";
export * from "./service/mobile-backend.service";
