/**
 * Reusable, Business-Agnostic GeoSphere Generic Tasks Engine SDK (@geosphere/tasks)
 *
 * Exposes framework-independent Task domain primitives, ITaskRepository abstraction,
 * TaskStateMachine, and TasksEngine.
 *
 * MUST NOT import Next.js path aliases (@/), OpenLayers, React, or vehicle-specific code.
 */

export * from "../../../core/tasks";
