/**
 * Reusable, Business-Agnostic GeoSphere Generic Security & Audit Engine SDK (@geosphere/security)
 *
 * Exposes framework-independent Security domain primitives, TenantContext guards,
 * RBAC permission matrix verification, correlation ID audit logging, PII redaction, and SecurityAuditEngine.
 *
 * MUST NOT import Next.js path aliases (@/), OpenLayers, React, or vehicle-specific code.
 */

export * from "../../../core/security";
